---
description: 'filter types.'
labels: ['Enum', 'Types']
---

import { filterTypes } from './client-types';

## Client type enum

An Enum of possible types.
These are types that should be parasable by form fields and tables etc.
