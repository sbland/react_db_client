---
description: 'A FilterManager component.'
labels: ['filter']
---

import { FilterPanel } from './filter-manager';
import { demoFiltersData, demoFieldsData, allTypeFilters, allTypeFieldsData } from './demoData';

## React Component for handling filters.

A react component for handling filters. Includes hooks and render components.

### Component usage
```js
  <FilterPanel
    filterData={allTypeFilters}
    addFilter={() => {}}
    deleteFilter={() => {}}
    updateFilter={() => {}}
    clearFilters={() => {}}
    showPanelOverride
    fieldsData={allTypeFieldsData}
  />
```

### Using props to customize the text

Modify the text to see it change live:
```js live
  <FilterPanel
    filterData={allTypeFilters}
    addFilter={() => {}}
    deleteFilter={() => {}}
    updateFilter={() => {}}
    clearFilters={() => {}}
    showPanelOverride
    fieldsData={allTypeFieldsData}
  />
```
