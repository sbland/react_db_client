import { renderHook, act } from '@testing-library/react-hooks';

import { demoFieldsData, demoTemplateData } from './demo-data';
import { getFieldsListFromTemplate, useGetFieldsData } from './map-fields';


describe('map fields', () => {
  test('should Implement test', () => {
    //
  });
});
