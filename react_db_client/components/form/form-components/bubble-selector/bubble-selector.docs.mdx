---
description: 'A BubbleSelector component.'
labels: ['field-component', 'multi-select', 'form']
---

import { BubbleSelector } from './bubble-selector';
import { BasicBubbleSelector } from './bubble-selector.composition';

## React Component for rendering selectable list items

Bubble selector for selecting multiple items in a list of options.

### Using props to customize the text

Modify the text to see it change live:
```js live
<BasicBubbleSelector />
```
