---
description: 'A FieldText component.'
labels: ['form_field']
---

import { FieldText } from './field-text';

## React Component for rendering text

A basic div that renders some text

### Component usage

```js
<FieldText
  uid="id"
  unit="UNIT"
  updateFormData={(v) => alert(v)}
  value="hello from FieldText"
/>
```

### Using props to customize the text

Modify the text to see it change live:

```js live
<FieldText
  uid="id"
  unit="UNIT"
  updateFormData={(k, v) => alert(`Changed: ${k}, val: ${v}`)}
  value="hello from FieldText"
/>
```
