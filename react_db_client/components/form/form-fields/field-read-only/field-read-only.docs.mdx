---
description: 'A FieldReadOnly component.'
labels: ['form_field']
---

import { FieldReadOnly } from './field-read-only';
import { filterTypes } from '@react_db_client/constants.client-types';

## React Component for rendering read only fields

A basic div that renders some text

### Using props to customize the text

Modify the text to see it change live:

```js live
<FieldReadOnly
  value="hello from FieldReadOnly"
  unit="TEXT"
  type={filterTypes.text}
  options={{}}
/>
```
