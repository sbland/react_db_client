import React, { useEffect } from 'react';

const SearchAndSelect = (props) => {
  // useEffect(() => {
  //   if (searchFunction) searchFunction(initialFilters);
  // }, [searchFunction, initialFilters]);
  return (<div />);
};

export default SearchAndSelect;
